spring:
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/nossa_biblioteca
    username: postgres
    password: Adna2009
    driverClassName: org.postgresql.Driver
